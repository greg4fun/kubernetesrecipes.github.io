---
/**
 * QuickAnswer Component - For GEO (Generative Engine Optimization)
 * 
 * This component provides a direct, concise answer at the top of recipe pages
 * to improve visibility in AI search results (ChatGPT, Perplexity, Gemini).
 * 
 * Usage in MDX:
 * <QuickAnswer>
 *   To fix CrashLoopBackOff, check the container logs with `kubectl logs <pod> --previous`
 *   and look for exit codes. Common causes are OOMKilled (code 137), missing configs (code 1),
 *   or failing health probes.
 * </QuickAnswer>
 * 
 * SEO Benefits:
 * - Provides direct answer for AI citations
 * - Featured snippet optimization
 * - Reduces bounce rate (immediate value)
 */

export interface Props {
  title?: string;
}

const { title = "Quick Answer" } = Astro.props;
---

<aside class="quick-answer not-prose my-6 bg-gradient-to-r from-blue-50 to-indigo-50 border-l-4 border-blue-500 rounded-r-lg p-5 shadow-sm">
  <div class="flex items-start gap-3">
    <div class="flex-shrink-0 text-2xl" aria-hidden="true">💡</div>
    <div>
      <h2 class="text-sm font-bold uppercase tracking-wider text-blue-800 mb-2">
        {title}
      </h2>
      <div class="text-slate-700 text-base leading-relaxed prose prose-slate prose-sm max-w-none
        prose-code:bg-blue-100 prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded prose-code:text-blue-800 prose-code:font-mono prose-code:text-sm
        prose-strong:text-slate-900">
        <slot />
      </div>
    </div>
  </div>
</aside>

<style>
  .quick-answer {
    /* Semantic markup for crawlers */
    --answer-type: "direct-answer";
  }
</style>
